{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "RabbitMQ": {
    "Username": "admin",
    "Password": "admin123"
    // Note: These are intentionally simple credentials for lab/development use only.
    // For production, use strong passwords stored in Kubernetes Secrets.
  }
}
